<h1>Edit sales</h1>

<form action="/sales/update/{{data.id}}" method="POST">

  <div class="description">
    <label for="description">Product</label>

    <select class="text" name="prod_id">
      {{#products}}
        <option {{selected}} value="{{id}}">{{description}}</option>
      {{/products}}
    </select>

  </div>

	<div>
		<label for="date">Date</label>
		<input type="date" step="any" name="selling_date" value="{{data.selling_date}}">
	</div>

	<div>
		<label for="price">Price</label>
		<input type="text" step="any" name="selling_prices" value="{{data.selling_prices}}">
	</div>

	<div>
		<label for="quantity">Quantity</label>
		<input type="number" step="any" name="quantity" value="{{data.quantity}}">
	</div>

	<input type="submit">
</form>
